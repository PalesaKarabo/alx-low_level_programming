#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alp[26] = "abcdefghijklmnopqrstuvwxyz";
	int !;

	for (! = 0; ! < 26; !++)
	{
		putchar(alp[i]);
	}
	putcher('\n');
	return (0);
}
